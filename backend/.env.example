# ============================================================
# Licitei — Backend (Elysia + TypeScript)
# Copie este arquivo para .env e preencha com seus valores.
# NUNCA commite o arquivo .env.
# ============================================================

# --- Servidor ---
PORT=3000
NODE_ENV=development

# --- Supabase ---
# Encontre em: Project Settings → API
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=<service_role_key>

# --- MongoDB Atlas ---
# Mesma instância usada pelo ETL
MONGO_URI=mongodb+srv://<usuario>:<senha>@cluster.mongodb.net/
MONGO_DB_NAME=licitei
MONGO_COLLECTION=contratacoes

# --- JWT ---
JWT_SECRET=<string-aleatoria-longa>
JWT_EXPIRES_IN=7d

# --- MCP (Track 3) ---
# URL do servidor MCP — usado pelo endpoint SSE (Sprint 2)
MCP_URL=http://localhost:8000
